export class Restaurant {
    id: number;
    name: string;
    rank: number;

constructor(id: number, name: string, rank: number) {
    this.id = id;
    this.name = name;
    this.rank = rank;
}
}

